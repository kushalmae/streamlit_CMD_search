apiVersion: v1
kind: Service
metadata:
  name: command-search
  labels:
    app: command-search
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: command-search