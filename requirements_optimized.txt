streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0

# Optional but recommended for fast CSV loading:
polars>=0.19.0       # 5-10x faster CSV loading
pyarrow>=13.0.0      # Required for Parquet format support
fastparquet>=0.8.0   # Alternative Parquet engine 